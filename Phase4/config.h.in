#pragma once

/* default number of ticks to run */
#define CFG_DEFAULT_TICKS    @DEFAULT_TICKS@

/* pointer mode: "shared", "pool", or "raw" */
#define CFG_DEFAULT_PTRMODE  "@DEFAULT_PTRMODE@"

/* whether to build with alignas(64) in MarketData */
#define CFG_ENABLE_ALIGN64   @ENABLE_ALIGN64_VALUE@

/* whether to build with MemoryPool allocator on by default */
#define CFG_ENABLE_POOL       @ENABLE_POOL_VALUE@

/* container layout: "multimap" or "flat" */
#define CFG_DEFAULT_LAYOUT   "@DEFAULT_LAYOUT@"
